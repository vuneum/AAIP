# AAIP Backend — pinned for reproducible builds
# To upgrade: run pip-compile or update manually then re-test.

# Web framework
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.28
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1

# Pydantic
pydantic==2.6.3
pydantic-settings==2.2.1

# HTTP
httpx==0.27.0
aiohttp==3.9.3

# Async job queue
celery[redis]==5.4.0
redis==5.0.3

# AI / Embeddings
openai==1.14.2
numpy==1.26.4

# Scheduling
apscheduler==3.10.4

# Utils
python-dotenv==1.0.1

# Optional: local embeddings (comment out to reduce image size)
# sentence-transformers>=2.3.1
# torch>=2.1.0

# Dev / Test
pytest==8.1.1
pytest-asyncio==0.23.5
aiosqlite==0.20.0   # in-memory DB for tests
